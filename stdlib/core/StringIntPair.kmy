class StringIntPair {
    let key: string = "";
    let value: int = 0;
    init(key: string, value: int) {
        this.key = key;
        this.value = value;
    }
    fun matches(key: string): bool {
        return streq(this.key, key);
    }
};
