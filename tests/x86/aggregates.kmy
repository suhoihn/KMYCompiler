class Counter {
    var value: int = 10;

    init(start: int) {
        this.value = start;
    }

    fun inc(): int {
        this.value += 1;
        return this.value;
    }
};

var c: Counter = new Counter(4);
print(c.inc());
print(c.inc());
