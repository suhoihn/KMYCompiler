class Counter {
    let value: int = 10;

    init(const start: int) {
        this.value = start;
    }

    fun inc(): int {
        this.value += 1;
        return this.value;
    }
};

let c: Counter = new Counter(4);
print(c.inc());
print(c.inc());
