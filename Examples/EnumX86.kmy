// Native x86 enum support: variants lower to stable integer ordinals.
enum Color {
    RED,
    GREEN,
    BLUE
};

var first: Color = Color::RED;
var second: Color = Color::GREEN;

print(first);
print(second);
print(Color::BLUE);
print(first == Color::RED);
print(second == Color::BLUE);
