// The source below is parsed by Examples/KmyParser.kmy, not executed.
let total: int = 1 + 2 * (9 - 5) / 2;
let cell: int* = malloc(-2);
*cell = total;
let alias: int* = &*cell;
*alias = *alias + 3;
