// The source below is parsed by Examples/KmyParser.kmy, not executed.
var total: int = 1 + 2 * (9 - 5) / 2;
var cell: int* = malloc(-2);
*cell = total;
var alias: int* = &*cell;
*alias = *alias + 3;
