// =====================================================
// SIMPLE FUNCTION
// =====================================================

fun add(a: int, b: int): int {
    return a + b;
}

print(add(1, 2)); // 3


// =====================================================
// FIRST-CLASS FUNCTION VALUE
// =====================================================

var f: (int, int) -> int = add;
print(f(3, 4)); // 7


// =====================================================
// SIMPLE CLOSURE
// =====================================================

fun makePrinter(x: int): () -> void {
    return fun(): void {
        print(x);
    };
}

var p: () -> void = makePrinter(42);
p();


// =====================================================
// CLOSURE RETURNING VALUE
// =====================================================

fun makeAdder(x: int): (int) -> int {
    return fun(y: int): int {
        return x + y;
    };
}

var add10: (int) -> int = makeAdder(10);
print(add10(5));


// =====================================================
// MULTIPLE CAPTURES
// =====================================================

fun makeSum(a: int, b: int): () -> int {
    return fun(): int {
        return a + b;
    };
}

var sum: () -> int = makeSum(3, 7);
print(sum());


// =====================================================
// UPVALUE CAPTURING UPVALUE
// =====================================================

// TODO: support () shorthand if parser dislikes it.
// e.g. () -> (() -> void)

fun outer(): () -> () -> void {
    var x: int = 100;

    return fun(): () -> void {
        return fun(): void {
            print(x);
        };
    };
}

var mid: () -> void = outer()();
mid();


// =====================================================
// CAPTURE + PARAM
// =====================================================

fun makeMul(x: int): (int) -> int {
    return fun(y: int): int {
        return x * y;
    };
}

var mul3: (int) -> int = makeMul(3);
print(mul3(7));


// =====================================================
// CAPTURE SHADOWING
// =====================================================

var v: int = 999;

fun shadowTest(): () -> void {
    var v: int = 123;

    return fun(): void {
        print(v);
    };
}

var s: () -> void = shadowTest();

s();
print(v);