for (let outer = 0; outer < 4; outer += 1) {
    let inner = 0;

    while (inner < 5) {
        inner += 1;

        if (inner == 2) {
            continue;
        }

        if (outer == 2) {
            if (inner == 4) {
                break;
            }
        }
    }
}