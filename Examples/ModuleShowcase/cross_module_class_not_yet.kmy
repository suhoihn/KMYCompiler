// Historical focused probe for qualified imported construction. This now
// parses and resolves; the automated equivalent is tests/x86/modules/
// cross_class_main.kmy, which keeps its dependency intentionally minimal.
import "library.kmy" as library;

var counter: library::Counter = new library::Counter(5);
print(counter.bump());
