fun makeFib(): () -> int {
    var a: int = 0;
    var b: int = 1;

    fun next(): int {
        var tmp: int = a + b;
        a = b;
        b = tmp;
        return a;
    }

    return next;
}

var fib: () -> int = makeFib();

for (var i: int = 0; i < 10; i += 1) {
    print(fib());
}