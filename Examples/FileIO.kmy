// Whole-file text I/O. readFile/writeFile close their handles internally.
var path: string = "build/kmy_file_io.tmp";
var message: string = "hello from KMY";

print(writeFile(path, message));
var loaded: string = readFile(path);
print(loaded);
print(strlen(loaded));
